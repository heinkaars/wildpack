# Server-side only (never shipped in the app bundle) — used by app/api/*+api.ts
OPENAI_API_KEY=sk-...

# Server-side only. Writes the shared species catalog, which clients are no
# longer allowed to write. Bypasses row level security, so it must never gain an
# EXPO_PUBLIC_ prefix. Supabase dashboard → Settings → API → service_role.
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Safe to ship in the app bundle: Row Level Security is what protects the data,
# not the secrecy of these values.
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
